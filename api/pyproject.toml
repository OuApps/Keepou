[project]
name = "keepou-api"
version = "0.0.0"
description = "Keepou — API FastAPI (Google Keep auto-hébergé)"
requires-python = ">=3.11"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]

[tool.pytest.ini_options]
testpaths = ["tests"]
