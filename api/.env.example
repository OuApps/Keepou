# Database — SQLite by default in dev; PostgreSQL recommended in prod.
DATABASE_URL=sqlite:///./keepou.db

# Session signing secret (signed cookie). Regenerate in prod.
SESSION_SECRET=dev-change-me

# Allowed origins (Vite front) for CORS, comma-separated.
CORS_ORIGINS=http://localhost:5173
